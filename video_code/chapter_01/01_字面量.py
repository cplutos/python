"""
1.所谓字面量，就是直接卸载代码中的具体指
2.python的字符串中可以包含任意字符，切必须使用引号抱起来
3.字符串的引号，可以是：单引号，双引号，三个单引号，三个双引号
4.卸载Python文件头部的字符串，会被自动识别为docstring（文档字符串）
5.文档字符串的主要作用是：对当前的python文件进行一些说明，且文档字符必须使用三个双引号
"""
"张三"
'23'
'54.3'

print(100)
print(200)
print(100 + 200)
