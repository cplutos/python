"""
%m.ns
m:
    1.字符串的最想宽度，位数不够会自动使用空格补全，位数小于字符串长度则不起作用
    2.正数是右对齐，负数是左对齐
n：
    精度控制，最多输出n个字符（若n大于实际字符串长度，则不起作用）
"""

name = '张三'
gender = '男'
weight = 76.3
age = 14

info = '我叫%4.2s，性别是%3.2s，体重是%.0f，年龄是%d' %(name,gender,weight,age)
print(info)